{
  "name": "vue-node-demo",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production node server.mjs",
    "dev": "NODE_ENV=development nodemon --env-file=.env server.mjs",
    "uat": "NODE_ENV=production nodemon --env-file=.env server.mjs"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
